import 'package:flutter/material.dart';

class ActualizarEstadoPantalla extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Actualizar estado del servicio")),
      body: Center(
          child: Text("Pantalla para actualizar el estado de un servicio")),
    );
  }
}
